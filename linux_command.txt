вывести конфиг
php-fpm -tt
отобразить используемую память 
free -hl
посмотреть память занимаемую одним процессом
ps -ylC php-fpm --sort:rss

Locate the binary, source, and manual-page files for a command
whereis php

инфо о процессоре
lscpu

диск 
df -h
du -h
iostat
iotop
lsblk - список дискровых устройств

vmstat статистики виртуальной памяти, потоков ядра, дисков, системных процессов, блоков ввода-вывода, прерываний, активности процессора 



Сеть
tcpdump – сниффер пакетов
netstat -a – мониторинг статистики входящих и исходящих сетевых пакетов, а также статистики интерфейсов
или ss



export добавляет перерменную env
env переменные среды окружения

lsof (от англ. LiSt of Open Files) — утилита, служащая для вывода информации о том, какие файлы используются теми или иными процессами


cat  выводит содержание файлов в окне терминала.
-b — нумеровать только строки с содержимым;
-E — выводить $ в конец каждой строки;
-n — нумеровать все строки;
-s – удалять повторы пустых строк;
-T — выводить табуляции в виде ^I.

chmod Команда chmod устанавливает права доступа к файлу или каталогу
chmod опции права /путь/к/файлу

chown Команда chown (change owner) позволяет менять владельца и группу владельца
chown -R user:users ./ex1
-c — детальный вывод всех выполняемых изменений;
-f — минимум информации (убирает ошибки из выдачи);
-h — изменять права символических ссылок, а файлы к которым они ведут не трогать;
-R — рекурсивная обработка всех подкаталогов;
-L — переходить по всем символическим ссылкам на директории.
